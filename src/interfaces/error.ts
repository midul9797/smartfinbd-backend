// Defines the structure of a generic error message
export type IGenericErrorMessage = {
  path: string | number; // The path or location where the error occurred
  message: string; // The error message itself
};
